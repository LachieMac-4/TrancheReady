<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1">
<title>Team • TrancheReady</title>
<style>
  :root{--ink:#E6EDFF;--muted:#95A7D0;--bg:#0A1630;--panel:#0B1733;--line:#213058;--radius:14px}
  body{margin:0;background:#0A1630;color:var(--ink);font-family:Inter,ui-sans-serif,system-ui}
  .wrap{max-width:800px;margin:28px auto;padding:0 16px}
  .card{background:#0B1733;border:1px solid var(--line);border-radius:var(--radius);box-shadow:0 18px 44px rgba(0,12,45,.35);padding:18px;margin-bottom:16px}
  .row{display:flex;gap:10px;flex-wrap:wrap}
  .btn{display:inline-flex;align-items:center;gap:8px;border-radius:12px;padding:12px 16px;border:1px solid var(--line);background:linear-gradient(90deg,#2455FF,#7AA3FF);color:white;cursor:pointer;font-weight:600}
  input{width:100%;padding:10px;border-radius:10px;border:1px solid var(--line);background:#0B1738;color:var(--ink)}
  .muted{color:var(--muted)}
  pre{white-space:pre-wrap;background:#0C1733;border:1px solid var(--line);border-radius:10px;padding:12px;overflow:auto}
</style>
</head>
<body>
<div class="wrap">
  <div class="card">
    <h1>Team invites</h1>
    <p class="muted">Team ID: <code><%= team_id %></code> • Build <%= build_id %></p>
    <div class="row">
      <button class="btn" id="invite">Create Invite Link</button>
      <input id="out" placeholder="Invite link will appear here…" readonly />
    </div>
  </div>
  <div class="card">
    <h2>How it works</h2>
    <pre>1) Click "Create Invite Link"
2) Send the link to a teammate
3) They open it → read-only seat cookie is set
4) They can access /app without paying (your team seat)</pre>
  </div>
</div>
<script>
document.getElementById('invite').onclick = async ()=>{
  const r = await fetch('/team/invite',{ method:'POST' });
  const j = await r.json();
  document.getElementById('out').value = j.invite_url || 'Failed to create link';
};
</script>
</body>
</html>
